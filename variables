#! vim ft=bash
# Complete the following list of attributes
PROJECT_NAME="sandisk_automatic_sync"
VENDOR_ID="0781"
PRODUCT_ID="75a0"
LOCAL_DIR="/home/pi/music/" # Has to end with slash
REMOTE_DIR="Music" # No trailing slash, relative to mount


# Do not modify these
LOG_FILE="/tmp/$PROJECT_NAME.log"
MOUNT_FILE="/mnt/$PROJECT_NAME"
NODE_FILE="/dev/$PROJECT_NAME"
PIPE_FILE="/tmp/$PROJECT_NAME"
ABSOLUTE_REMOTE_DIR="${MOUNT_FILE}/${REMOTE_DIR}"
